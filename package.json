{
  "name": "zk_l3_graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ root/zk_l3_graph",
    "create-local": "graph create --node http://localhost:8020/ root/zk_l3_graph",
    "remove-local": "graph remove --node http://localhost:8020/ root/zk_l3_graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 root/zk_l3_graph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.68.5",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
